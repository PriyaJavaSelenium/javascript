/* 2.For this array task, we provide you with a starting array,
 and you will work in somewhat the opposite direction. You need to:
Remove the last item in the array.
Add two new names to the end of the array.
Finally, join the array items together in a single string called myString, with a separator of "-".
*/

var a = ["Aa","Bb","Cc","Dd","Ee"];
a.pop();
var b = ["1","2","3","4","5"]
b.push("66","77")
console.log("1. Remove the last item: " + a)
console.log("2. Add two new names to the end of the array: " + b);
var myString = a + " - " + b
console.log("3. Join the array items together in a single string called myString: " + myString)




